// Placeholder test — Task 1 scaffold verification.
// Later tasks replace this with focused widget and unit tests.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('scaffold placeholder — sanity check', () {
    expect(1 + 1, 2);
  });
}
